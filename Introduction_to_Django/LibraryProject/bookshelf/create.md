# Create Operation: Adding a Book to the Database

To create a new book entry using Django's ORM, follow these steps:

## Step 1: Open the Django Shell
```bash
python manage.py shell
# Create Operation: Adding a Book to the Database

To create a new book entry using Django's ORM, follow these steps:

## Step 1: Open the Django Shell
```bash
python manage.py shell
